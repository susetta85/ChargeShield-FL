{
  "_comment": "ChargeShield-FL — client-side config, NVFLARE 2.7.2 (2026-07-22, NON verificato). L'Executor custom (chargeshield_executor.py) avvolge AutoencoderTrainer.train_local() — la STESSA classe usata da scripts/run_experiments.py in simulazione single-process. FIX 2026-07-22 (review A1): questo stesso file viene deployato a TUTTI i siti (meta.json deploy_map='@ALL'), quindi 'cluster_id' qui sotto è solo un FALLBACK — a runtime ChargeShieldExecutor._setup() deriva cluster_id dal nome del sito NVFLARE (fl_ctx.get_identity_name(), che nvflare/project.yml garantisce essere highway/urban/residential/corporate), non da questo valore. dataset_path resta condiviso da tutti i client (fase 1, filtro client-side — vedi limitazioni in docs/NVFlareIntegration.md).",
  "format_version": 2,
  "executors": [
    {
      "tasks": ["train"],
      "executor": {
        "path": "chargeshield_executor.ChargeShieldExecutor",
        "args": {
          "cluster_id": "highway",
          "input_dim": 6,
          "lr": 0.001,
          "epochs": 50,
          "batch_size": 32,
          "proximal_mu": 0.01,
          "seed": 42,
          "dataset_path": "datasets/acn/jpl/acndata_sessions_2019.json"
        }
      }
    }
  ],
  "task_result_filters": [],
  "task_data_filters": []
}
