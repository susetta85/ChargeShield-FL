\begin{figure}[t]
\centering
\hspace*{0.1cm}
\begin{tikzpicture}[
  box/.style={
    draw,
    rounded corners,
    minimum height=0.68cm,
    minimum width=1.65cm,
    align=center,
    font=\scriptsize
  },
  hub/.style={
    draw,
    circle,
    minimum size=1.0cm,
    align=center,
    font=\scriptsize\bfseries
  },
  consumer/.style={
    draw,
    rounded corners,
    minimum height=0.82cm,
    minimum width=1.75cm,
    align=center,
    font=\scriptsize
  },
  arr/.style={-Latex, thick}
]

% ------------------------------------------------
% Purdue-level producers
% ------------------------------------------------

\node[box] (local)
  {Local trainer\\
   \tiny Purdue L1 (raw update)};

\node[box, right=0.30cm of local] (privatize)
  {Clip + DP noise\\
   \tiny Purdue L2 (privatized)};

\node[box, right=0.30cm of privatize] (aggregate)
  {FedAvg aggregator\\
   \tiny round aggregated};

% ------------------------------------------------
% ML Plane hub
% ------------------------------------------------

\node[hub, below=0.9cm of privatize] (hub)
  {ML Plane\\hub};

% ------------------------------------------------
% Three consumers
% ------------------------------------------------

\node[consumer, below=1.55cm of hub, xshift=-3.5cm] (auditor)
  {Privacy Auditor\\
   \tiny in-line, budget telemetry\\
   \tiny \S3.2};

\node[consumer, below=1.55cm of hub, xshift=-0.65cm] (byz)
  {ByzantineDetector\\
   \tiny in-line, integrity\\
   \tiny \S3.3};

\node[consumer, below=1.55cm of hub, xshift=2.5cm] (attacks)
  {Attack suite\\
   \tiny \emph{offline}, empirical auditing\\
   \tiny Yeom / Shadow / LiRA \quad \S3.4};

% ------------------------------------------------
% Connections
% ------------------------------------------------

\draw[arr] (local) -- (hub);
\draw[arr] (privatize) -- (hub);
\draw[arr] (aggregate) -- (hub);

\draw[arr] (hub) -- (auditor);
\draw[arr] (hub) -- (byz);
\draw[arr, dashed] (hub) -- (attacks);

% ------------------------------------------------
% Offline note
% ------------------------------------------------

\node[
  font=\scriptsize,
  align=center,
  below=0.15cm of byz
] {
  \emph{dashed = buffered to \texttt{round\_data}, consumed after training ends}
};

\end{tikzpicture}

\caption{The ML Plane publishes three Purdue-labeled event types per
round to a central hub, which fans them out to three independent
consumers. The Privacy Auditor and ByzantineDetector process events
in-line, without altering the training loop; the attack suite buffers
the same events and runs offline, after training completes
(Section~\ref{sec:framework-attacks}).}
\label{fig:mlplane}
\end{figure}